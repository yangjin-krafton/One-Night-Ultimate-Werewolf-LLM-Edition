# TODO / 구현 현황 정리

이 문서는 예전 서버 기반 구현 메모를 치운 뒤, 현재 저장소의 정적 앱 기준 체크리스트만 남겨 둔 버전입니다.

## 현재 기준

- 런타임 앱은 `public/app.js` 중심의 정적 HTML/CSS/JS 구조입니다.
- 방 코드는 네트워크 방 식별자가 아니라, 시나리오/에피소드/인원/덱 조합을 인코딩한 공유 코드입니다.
- 음성은 `public/assets/voices/`의 생성 자산을 우선 사용하고, 없으면 브라우저 TTS fallback을 사용합니다.

## 다음 작업 제안

- [ ] `README.md`를 정적 앱 기준으로 더 압축해서 정리
- [ ] 시나리오별 음성 자산 누락 여부를 스크립트로 검사
- [ ] `public/app.js`의 시나리오/덱 관련 상수를 별도 파일로 분리할지 검토
- [ ] 브라우저 `speechSynthesis` fallback 동작을 모바일 환경에서 다시 확인

## 메모

- 예전 실시간 동기화 기준 TODO는 현재 구조와 맞지 않아 제거했습니다.
- TTS 관련 스크립트는 그대로 유지합니다. 여기서 말하는 "서버"는 게임 서버가 아니라 외부 음성 합성 API 서버입니다.
